Правило Де Моргана (одно из правил вероятностей)
Оно необходимо, если у нас есть большая инверсия над выражением.
!(а и в) = !(а) или !(в);
!(а) и !(в) = !(а или в)
Пул строк – это набор строк, который хранится в памяти Java heap.
Чтобы не допустить роста объёма кода, компилятор хранит литеральную строку в метаданных модуля только в одном экземпляре.
Все упоминания этой строки в исходном коде компилятор заменяет ссылками на её экземпляр в метаданных.
